{
  "name": "sam-database-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --project demo-sam-security --config ../firebase.json --only database \"node --test tests/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
